PORT=8080
JWT_SECRET=change_me_in_prod
PICSUM_BASE=https://picsum.photos
LOG_LEVEL=dev

# Note: Postgres is no longer required at runtime. If you need to migrate data from an
# existing Postgres database into DynamoDB, use the migration script at
# src/scripts/migrate_postgres_to_dynamo.js which still depends on DATABASE_URL.
